import styles from '../styles/Home.module.css';

const Home = () => (
  <div className={styles.container}>
    <h1 className={styles.title}>Welcome to Novel Assistant!</h1>
  </div>
);

export default Home;
